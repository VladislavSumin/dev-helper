[versions]
vs-core = "0.1.0"
adam = "0.4.3"

[libraries]
# vs core
vs-compose = { module = "ru.vs.core:compose", version.ref = "vs-core" }
vs-coroutines = { module = "ru.vs.core:coroutines", version.ref = "vs-core" }
vs-decompose = { module = "ru.vs.core:decompose", version.ref = "vs-core" }
vs-di = { module = "ru.vs.core:di", version.ref = "vs-core" }
vs-logging-core = { module = "ru.vs.core:logging", version.ref = "vs-core" }
vs-logging-slf4j = { module = "ru.vs.core:logging-slf4j", version.ref = "vs-core" }
vs-navigation = { module = "ru.vs.core:navigation", version.ref = "vs-core" }
vs-uikit = { module = "ru.vs.core:uikit", version.ref = "vs-core" }

adam = { module = "com.malinskiy.adam:adam", version.ref = "adam" }
